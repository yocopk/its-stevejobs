#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/python$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/python$exe"  "$basedir/../clang-format/bin/git-clang-format" $args
  } else {
    & "$basedir/python$exe"  "$basedir/../clang-format/bin/git-clang-format" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "python$exe"  "$basedir/../clang-format/bin/git-clang-format" $args
  } else {
    & "python$exe"  "$basedir/../clang-format/bin/git-clang-format" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
